import { NavclickDirective } from './navclick.directive';

describe('NavclickDirective', () => {
  it('should create an instance', () => {
    const directive = new NavclickDirective();
    expect(directive).toBeTruthy();
  });
});
